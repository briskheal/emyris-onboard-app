[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --omit=dev"]

[phases.build]
cmds = ["echo 'Frontend already pre-compiled in frontend/dist/'"]

[start]
cmd = "node server.js"
