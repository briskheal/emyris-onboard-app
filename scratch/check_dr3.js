const { XlDoctor, XlUser } = require('../db'); async function check() { const u = await XlUser.findOne({ where: { employeeId: 'EMYFE118' } }); console.log('user uid:', u?.uid, 'email:', u?.email); const drs3 = await XlDoctor.findAll({ where: { employeeId: u?.email } }); const drs4 = await XlDoctor.findAll({ where: { userAllotted: u?.uid } }); console.log('by email:', drs3.length, 'by uid:', drs4.length); } check();
