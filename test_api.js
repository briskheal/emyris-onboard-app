const { XlTourProgram } = require('./db'); async function run() { try { const tps = await XlTourProgram.findAll({ where: { month: 'september', year: '2026' } }); console.log('Found:', tps.length); if (tps.length > 0) { console.log('Status:', tps[0].status); console.log('Entries length:', JSON.parse(tps[0].entries).length); const match = JSON.parse(tps[0].entries).find(e => e.date === '2026-09-05'); console.log('Match for 05:', match); } } catch(e) { console.error('ERROR:', e.message); } process.exit(); } run();
